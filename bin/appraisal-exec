#!/usr/bin/env sh

# Usage example
# bin/appraisal-exec 8.0 rspec (translates to bundle exec appraisal ar-8.0 rspec)

set -o pipefail
IFS=$'\n\t'

ar_version=$1
shift 1
ar_args="$@"

bundle exec appraisal ar-$ar_version $ar_args

unset ar_version
unset ar_args
